#include <stdio.h>

// Single line comment

/*
Multi
Line
Comment
*/

int main(int argc, char *args[])
/* For the function to be totally complete it needs to return an int and take two 
parameters, an int for the argument count and an array of char * strings for the arguments */
{
	int distance = 100;

	printf("You are %d miles away.\n", distance);

	return 0;
}
